---
title: "Run Report: TUI and Server enhancements"
date: "2026-06-21"
---

# Execution Report

- Added a `Port` field to the configuration.
- Added a `serve` command to the CLI to run an `http.FileServer` on the `OutputDir`.
- Implemented Bubbletea's `tea.WithAltScreen()` so the TUI takes over the terminal window and clears when done.
- Added a "Serve Site" option to the TUI which runs the local server in the background and displays the dancing mascot.
