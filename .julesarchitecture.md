## 2026-06-25 Graph File Writing Extraction

Extracted `graph.json` and `backlinks.json` writing logic from `internal/generator/generator.go` and `internal/sitedata/write.go` to a dedicated `WriteGraphFiles(outputDir string, g Graph, backlinks map[string][]string) error` function in the new `internal/graph/write.go` file.

This change aligns with the refactor-one-seam routine, centralizing the handling of graph-specific data (including sorting backlinks for deterministic output) to the `internal/graph` package, utilizing the existing `jsonutil.WriteJSON` helper. The `sitedata.Write` function now only handles `meta.json`.

### Verification Note
Upon review, it is confirmed that the `refactor-one-seam` routine targeting the extraction of `graph.json` and `backlinks.json` writing logic from `internal/generator/generator.go` into `internal/graph/write.go` is already completely implemented, correctly utilizing the `jsonutil.WriteJSON` helper, and tested.

## Refactor One Seam: internal/markdown
* **Date:** 2026-06-19
* **Change:** Extracted the `goldmark` markdown engine instantiation and configuration from `internal/generator/generator.go` into a new `internal/markdown` package.
* **Reason:** Reduce coupling and complexity inside the core static site generator build loop. Isolates the configuration of the markdown renderer (including custom inline parsers and AST transformers).
